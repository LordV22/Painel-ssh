[phases.setup]
nixPkgs = ["php", "php82Extensions.mysqli", "php82Extensions.pdo", "php82Extensions.mbstring", "php82Extensions.curl", "php82Extensions.gd"]

[start]
cmd = "php -S 0.0.0.0:\"$PORT\" -t ."
