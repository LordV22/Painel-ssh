[phases.setup]
nixPkgs = ["php", "phpExtensions.mysqli", "phpExtensions.pdo", "phpExtensions.pdo_mysql", "phpExtensions.mbstring", "phpExtensions.curl", "phpExtensions.gd"]

[start]
cmd = "php -S 0.0.0.0:\"$PORT\" -t ."
