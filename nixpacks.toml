[phases.setup]
nixPkgs = ["php", "phpExtensions.pdo_mysql", "phpExtensions.mysqli", "phpExtensions.pdo", "phpExtensions.mbstring", "phpExtensions.curl", "phpExtensions.gd"]

[start]
cmd = "php -S 0.0.0.0:\"$PORT\" -t ."
